var currentVersion = "3.2.0";

function initializeVariables() {
    const myVars = {
        loaded: false,
        onGamePage: false,
        forkColor: "#ff6b6b",
        skewerColor: "#4ecdc4",
        pinColor: "#ffe66d",
        discoveredAttackColor: "#a8dadc"
    };
    return myVars;
}
