var currentVersion = "1.9.3";

function initializeVariables() {
    const myVars = {
        delay: 0.1,
        loaded: false,
        onGamePage: false,
        lastValue: 3,
        blunderRate: 0.2,
        bestMoveColor: "#5b8c5a",
        intermediateMoveColor: "#ffa500"
    };
    return myVars;
}
